import { ExperienceFilterPipe } from './experience-filter.pipe';

describe('ExperienceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
